fn main() {
    // integer op
    let x = 10;
    let y = 4;

    println!("{} / {} = {}", x, y, x / y);

    println!("{} % {} = {}", x, y, x % y);

    // float op
    let x = 10.0;
    let y = 4.0;
    println!("{} / {} = {}", x, y, x / y);
}
